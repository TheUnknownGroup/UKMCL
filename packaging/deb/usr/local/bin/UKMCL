#!/bin/bash
if [ ! -f "$HOME/.ukmcl_install" ]; then
    echo "Running setup.."
    sudo dpkg -i UKMCL-Linux-Installer-en_US+0.0.0.deb
    sudo chmod +x /usr/share/PostInst
    PostInst
    touch "$HOME/.ukmcl_install"
fi

java -jar /usr/share/UKMCL/*.jar
